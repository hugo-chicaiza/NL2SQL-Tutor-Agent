----------- Inicializar servicions venv
cd ~/TrainingSQL/api
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

agregar venv a .dockerignore
which python
cambiar de interprete

----------- run in venv
python -m app.temporal_testing
python -c "import os; print(os.getenv('OPENROUTER_API_KEY'))"
SQL_ECHO=false python -m app.temporal_testing

docker exec -it sql_agent_api bash


Superuser DB trainer trainer
user read ai_reader ai_pass



sudo apt update && sudo apt install tree -y

tree /home/matthew/TrainingSQL/Api_start -I '__pycache__|venv|.venv|env|.git' > /home/matthew/TrainingSQL/Api_start/arbol_proyecto.txt

