----------- Inicializar servicions venv
cd ~/TrainingSQL/api
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

agregar venv a .dockerignore
which python
cambiar de interprete

----------- run in venv
python -m app.temporal_testing

SQL_ECHO=false python -m app.ttemporal_testing

docker exec -it sql_agent_api bash


Superuser DB trainer trainer
user read ai_reader ai_pass

